name: Build Flutter Android, iOS, and macOS

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      environment:
        description: "Select environment to build"
        required: true
        default: "dev"
        type: choice
        options:
          - dev
          - staging
          - prod

permissions:
  contents: write
  actions: read

env:
  XCODE_VERSION: "26.6"
  BUILD_ENV: ${{ github.event.inputs.environment || 'dev' }}

jobs:
  build-android:
    runs-on: macos-26
    environment: ${{ github.event.inputs.environment || 'dev' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Generate environment files
        uses: ./.github/actions/generate-env-files
        with:
          environment: ${{ env.BUILD_ENV }}
          dart_define_base64: ${{ secrets.DART_DEFINE_BASE64 }}
          google_services_base64: ${{ secrets.GOOGLE_SERVICES_BASE64 }}
          google_service_info_base64: ${{ secrets.GOOGLE_SERVICE_INFO_BASE64 }}

      - name: Get version from pubspec.yaml
        id: get_version
        uses: ./.github/actions/get-pubspec-version

      - name: Decode keystore and create jks and properties file for signing the app
        run: |
          sed -i '' -e 's/signingConfigs.getByName("debug")/signingConfigs.getByName("release")/' app/build.gradle.kts
          echo "$PLAY_STORE_UPLOAD_KEY" | base64 --decode > app/keystore.jks
          echo "storeFile=keystore.jks" >> key.properties
          echo "keyAlias=$KEYSTORE_KEY_ALIAS" >> key.properties
          echo "storePassword=$KEYSTORE_STORE_PASSWORD" >> key.properties
          echo "keyPassword=$KEYSTORE_KEY_PASSWORD" >> key.properties
        env:
          PLAY_STORE_UPLOAD_KEY: ${{ secrets.PLAY_STORE_UPLOAD_KEY }}
          KEYSTORE_KEY_ALIAS: ${{ secrets.KEYSTORE_KEY_ALIAS }}
          KEYSTORE_KEY_PASSWORD: ${{ secrets.KEYSTORE_KEY_PASSWORD }}
          KEYSTORE_STORE_PASSWORD: ${{ secrets.KEYSTORE_STORE_PASSWORD }}
        working-directory: android

      # Set up Java for Android build
      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: "zulu"
          java-version: "17"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          flutter-version-file: pubspec.yaml

      - name: Setup FlutterFire
        run: dart pub global activate flutterfire_cli

      # Build Android APK. One APK per ABI rather than a fat one: the arm64
      # build is what every phone and tablet installs, and armeabi-v7a is there
      # for the 32-bit ARM hardware most Google TV / Android TV boxes still
      # ship, which cannot install the arm64 APK at all.
      #
      # Two passes rather than `--split-per-abi`, which offsets the versionCode
      # per ABI: the arm64 artifact keeps the exact versionCode and asset name
      # it has always had, so nothing already installed sees a surprise.
      - name: Build APK (arm64-v8a)
        run: |
          flutter build apk --release \
            --target-platform android-arm64 \
            --dart-define-from-file envs/${{ env.BUILD_ENV }}/dart-define.env \
            --obfuscate --split-debug-info=build/obfuscate/arm64-v8a
          mkdir -p build/release-apks
          mv build/app/outputs/flutter-apk/app-release.apk \
            build/release-apks/app-release-arm64-v8a.apk

      - name: Build APK (armeabi-v7a)
        run: |
          flutter build apk --release \
            --target-platform android-arm \
            --dart-define-from-file envs/${{ env.BUILD_ENV }}/dart-define.env \
            --obfuscate --split-debug-info=build/obfuscate/armeabi-v7a
          mv build/app/outputs/flutter-apk/app-release.apk \
            build/release-apks/app-tv-armeabi-v7a.apk

      # Three assets from two files.
      #
      # Versions of the app already installed out there update themselves by
      # downloading the *first* asset whose name ends in `.apk`, with no idea
      # ABIs exist — and the GitHub API orders a release's assets by name, which
      # it documents nowhere. So the names are chosen to make every ordering
      # safe: `app-release*.apk` is always the arm64 build, and the 32-bit one
      # is `app-tv-...`, which sorts behind both of them. Whatever an old phone
      # reaches for first, it reaches for arm64.
      #
      # `app-release.apk` keeps the exact name those older versions have always
      # found; the suffixed pair is what the current updater chooses between.
      - name: Upload APK to Release
        uses: svenstaro/upload-release-action@2.11.5
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: build/release-apks/app-release-arm64-v8a.apk
          asset_name: app-release.apk
          tag: ${{ steps.get_version.outputs.version }}
          release_name: ${{ steps.get_version.outputs.version }}
          target_commit: ${{ github.sha }}
          overwrite: true

      - name: Upload arm64-v8a APK to Release
        uses: svenstaro/upload-release-action@2.11.5
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: build/release-apks/app-release-arm64-v8a.apk
          asset_name: app-release-arm64-v8a.apk
          tag: ${{ steps.get_version.outputs.version }}
          release_name: ${{ steps.get_version.outputs.version }}
          target_commit: ${{ github.sha }}
          overwrite: true

      - name: Upload armeabi-v7a (TV) APK to Release
        uses: svenstaro/upload-release-action@2.11.5
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: build/release-apks/app-tv-armeabi-v7a.apk
          asset_name: app-tv-armeabi-v7a.apk
          tag: ${{ steps.get_version.outputs.version }}
          release_name: ${{ steps.get_version.outputs.version }}
          target_commit: ${{ github.sha }}
          overwrite: true

  build-ios:
    runs-on: macos-26
    environment: ${{ github.event.inputs.environment || 'dev' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Generate environment files
        uses: ./.github/actions/generate-env-files
        with:
          environment: ${{ env.BUILD_ENV }}
          dart_define_base64: ${{ secrets.DART_DEFINE_BASE64 }}
          google_services_base64: ${{ secrets.GOOGLE_SERVICES_BASE64 }}
          google_service_info_base64: ${{ secrets.GOOGLE_SERVICE_INFO_BASE64 }}

      - name: Get version from pubspec.yaml
        id: get_version
        uses: ./.github/actions/get-pubspec-version

      - name: Set XCode version
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: ${{ env.XCODE_VERSION }}

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          flutter-version-file: pubspec.yaml

      - name: Setup FlutterFire
        run: dart pub global activate flutterfire_cli

      - name: Prepare iOS dependencies
        run: |
          flutter pub get
          bash scripts/patch-easy-video-editor.sh

      # Build an unsigned iOS app and package it as an IPA.
      # This artifact must be signed later before it can be installed on a device
      # or distributed through the App Store.
      - name: Build unsigned IPA
        run: |
          flutter build ios --release --no-codesign \
            --dart-define-from-file envs/${{ env.BUILD_ENV }}/dart-define.env \
            --obfuscate --split-debug-info=build/obfuscate

          mkdir -p build/ios/ipa/Payload
          ditto build/ios/iphoneos/Runner.app build/ios/ipa/Payload/Runner.app
          cd build/ios/ipa
          zip -qry do_x.ipa Payload

      # Upload IPA as release asset
      - name: Upload IPA to Release
        uses: svenstaro/upload-release-action@2.11.5
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: build/ios/ipa/do_x.ipa
          asset_name: do_x.ipa
          tag: ${{ steps.get_version.outputs.version }}
          release_name: ${{ steps.get_version.outputs.version }}
          target_commit: ${{ github.sha }}
          overwrite: true

  build-macos:
    runs-on: macos-26
    environment: ${{ github.event.inputs.environment || 'dev' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Generate environment files
        uses: ./.github/actions/generate-env-files
        with:
          environment: ${{ env.BUILD_ENV }}
          dart_define_base64: ${{ secrets.DART_DEFINE_BASE64 }}
          google_services_base64: ${{ secrets.GOOGLE_SERVICES_BASE64 }}
          google_service_info_base64: ${{ secrets.GOOGLE_SERVICE_INFO_BASE64 }}

      - name: Get version from pubspec.yaml
        id: get_version
        uses: ./.github/actions/get-pubspec-version

      - name: Set XCode version
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: ${{ env.XCODE_VERSION }}

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          flutter-version-file: pubspec.yaml

      - name: Build macOS app
        run: |
          # Create a temporary xcconfig to disable signing
          echo "CODE_SIGNING_ALLOWED=NO" > temp.xcconfig
          echo "CODE_SIGNING_REQUIRED=NO" >> temp.xcconfig
          echo "CODE_SIGN_IDENTITY=" >> temp.xcconfig
          echo "PROVISIONING_PROFILE_SPECIFIER=" >> temp.xcconfig
          
          export XCODE_XCCONFIG_FILE=$(pwd)/temp.xcconfig
          
          flutter build macos --release \
            --dart-define-from-file envs/${{ env.BUILD_ENV }}/dart-define.env \
            --obfuscate --split-debug-info=build/obfuscate

      - name: Package DMG
        run: |
          app_path="build/macos/Build/Products/Release/Do X.app"
          staging_path="build/macos/dmg"

          test -d "$app_path"
          mkdir -p "$staging_path"
          ditto "$app_path" "$staging_path/Do X.app"
          ln -s /Applications "$staging_path/Applications"

          hdiutil create \
            -volname "Do X" \
            -srcfolder "$staging_path" \
            -format UDZO \
            -ov \
            "build/macos/do-x.dmg"

      - name: Upload DMG to Release
        uses: svenstaro/upload-release-action@2.11.5
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: build/macos/do-x.dmg
          asset_name: do-x.dmg
          tag: ${{ steps.get_version.outputs.version }}
          release_name: ${{ steps.get_version.outputs.version }}
          target_commit: ${{ github.sha }}
          overwrite: true

  update-release-notes:
    needs:
      - build-android
      - build-ios
      - build-macos
    runs-on: ubuntu-latest
    steps:
      - name: Checkout full history
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Get version from pubspec.yaml
        id: get_version
        uses: ./.github/actions/get-pubspec-version

      - name: Generate release notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CURRENT_TAG: ${{ steps.get_version.outputs.version }}
        run: |
          previous_tag="$({
            gh api "repos/${GITHUB_REPOSITORY}/releases?per_page=100" \
              --jq '.[] | select(.draft == false and .prerelease == false) | .tag_name'
          } | while IFS= read -r tag; do
            if [[ "$tag" != "$CURRENT_TAG" ]]; then
              echo "$tag"
              break
            fi
          done)"

          if [[ -n "$previous_tag" ]]; then
            git rev-parse --verify "${previous_tag}^{commit}"
            commit_range="${previous_tag}..${GITHUB_SHA}"
          else
            commit_range="${GITHUB_SHA}"
          fi

          commit_notes="$(git log --no-merges --format='- %s (`%h`)' "$commit_range")"

          {
            echo "## Changes"
            echo
            if [[ -n "$commit_notes" ]]; then
              echo "$commit_notes"
            else
              echo "No non-merge commits since the previous release."
            fi
          } > release-notes.md

          gh release edit "$CURRENT_TAG" \
            --title "$CURRENT_TAG" \
            --notes-file release-notes.md
